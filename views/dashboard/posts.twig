{% extends "base.twig" %}

{% block content %}
<h1>Posts</h1>
<ul>
    {% for post in posts %}
    <li>
        <h2>{{ post.title }}</h2>
        <p>{{post.content}}</p>
        <small>Posted on: {{ post.created_at }}</small>
    </li>
    {% endfor %}
</ul>
{% endblock %}